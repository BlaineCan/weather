html
    head
    body
        main
            h1 Weather
            h3 Type in the search bar below to grab the weather. 
            div.container
                input.search(type="text")
                input.search(type="submit" value="Submit")
                
        