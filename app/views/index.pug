html
    head
    body
        main
            h1 Weather
            h3 Type in the search bar below to grab the weather. 
            div.container
            form(action="/weather", method="get")
                input.search(type="text")
                button.search-button(type="submit" ) Submit
                
        